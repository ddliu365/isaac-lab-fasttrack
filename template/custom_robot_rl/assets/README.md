Put `my_robot.urdf` and its `meshes/` here. Paths inside the URDF must be relative (`meshes/x.stl`) or `package://`.
Run `python3 /opt/fasttrack/tools/urdf_lint.py my_robot.urdf` and fix every E* before training.
